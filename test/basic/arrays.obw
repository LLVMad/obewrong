module arrays 

class Main is
  this() is
    // no initializer
    var arr : Array[Integer, 5]
    arr := [1, 2, 3, 4, 5]

    printf("- integer array created: ")

    var i : Integer := 0
    for i, i.Less(5), i := i.Plus(1) is
      printf("%d ", i)
    end

    // with initializer
    // var arr2 : Array[Integer] := [1, 2, 0, 0]

    // element manipulation
    // arr2[0] := 5
    // var el : Integer := arr2[0]
  end
end
